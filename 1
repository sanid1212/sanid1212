function get_system_info()
    local cmd = 'systeminfo'
    local handle = io.popen(cmd)
    local result = handle:read('*a')
    handle:close()
    return result
end

-- Example usage:
local system_info = get_system_info()
print(system_info)
