List Dimensions: This query lists all the dimensions in the specified cube.

dmx
Copy code

SELECT * FROM $SYSTEM.DBSCHEMA_DIMENSIONS WHERE CUBE_NAME = 'YourCubeName'
List Measures: This query lists all the measures in the specified cube.

dmx
Copy code

SELECT * FROM $SYSTEM.DBSCHEMA_MEASURES WHERE CUBE_NAME = 'YourCubeName'
List Hierarchies: This query lists all the hierarchies in the specified cube.

dmx
Copy code

SELECT * FROM $SYSTEM.DBSCHEMA_HIERARCHIES WHERE CUBE_NAME = 'YourCubeName'
List Levels: This query lists all the levels in the specified cube.

dmx
Copy code

SELECT * FROM $SYSTEM.DBSCHEMA_LEVELS WHERE CUBE_NAME = 'YourCubeName'
List Members: This query lists all the members in the specified cube.

dmx
Copy code

SELECT * FROM $SYSTEM.DBSCHEMA_MEMBERS WHERE CUBE_NAME = 'YourCubeName'
